// 이 파이프라인은 더 이상 사용되지 않습니다.
// 모든 배포는 'ict05_final_admin-kw' 프로젝트의 Jenkins 파이프라인에서 통합 관리됩니다.

pipeline {
    agent any
    stages {
        stage('Notification') {
            steps {
                echo "========================================================================"
                echo "DEPRECATED: This pipeline is no longer in use."
                echo "All services are now built and deployed by the 'ict05_final_admin-kw' pipeline."
                echo "Please run the 'admin-kw' Jenkins job to deploy all services."
                echo "========================================================================"
            }
        }
    }
}